package com.mentors.applicationstarter.Model;

public class Invoice {

    /*
    Invoice {
    id
    user (nullable if company invoice)
    company (nullable if individual invoice)
    amount
    issuedDate
    dueDate
    paid (bool)
    pdfLink (optional)
    relatedPurchases [Purchase]  // For user
    billedUsersCount (for company)
}
*/
}
